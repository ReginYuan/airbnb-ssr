import api from "@/api/index";
import { useStore } from '@/store/index'
export default function houseOperates(router: any, params: any) {

  // //获取全部名宿数据接口方法
  // const HouseList = () => {

  //   return new Promise((resolve) => {
  //     //获取store对象
  //     const store = useStore()
  //     api.fetchRoomList().then(res => {
  //       store.commit("saveHouseList", res.data);
  //     })
  //     resolve(true)
  //   })
  // };
  // return {
  //   HouseList
  // }
}
